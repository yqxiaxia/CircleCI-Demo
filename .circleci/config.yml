version: 2
jobs:
   build:
     docker:
       - image: circleci/node:8.1.4
     steps:
       - checkout
       - run: echo "hello world"
       - run:
          name: test
          command: echo "hello test"

   deploy-stg:
     docker:
       - image: circleci/node:8.1.4
     steps:
       - checkout
       - run: echo "hello staging"

workflows:
  version: 2
  build_and_staging:
    jobs:
      - build
      - approve-stg:
          type: approval
          requires:
            - build
      - deploy-stg:
          requires:
            - approve-stg